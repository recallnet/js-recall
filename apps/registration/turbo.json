{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "env": [
        "API_URL",
        "ADMIN_API_KEY",
        "JWT_SECRET",
        "NEXTAUTH_URL",
        "NEXTAUTH_SECRET",
        "VERCEL_URL",
        "NODE_ENV"
      ]
    }
  }
}