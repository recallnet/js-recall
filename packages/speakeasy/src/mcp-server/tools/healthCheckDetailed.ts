/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { healthCheckDetailed } from "../../funcs/healthCheckDetailed.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$healthCheckDetailed: ToolDefinition = {
  name: "health-check-detailed",
  description: `Detailed health check

Check if the API and all its services are running properly`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await healthCheckDetailed(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
