/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { competitionGetLeaderboard } from "../../funcs/competitionGetLeaderboard.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiCompetitionLeaderboardRequest$inboundSchema,
};

export const tool$competitionGetLeaderboard: ToolDefinition<typeof args> = {
  name: "competition-get-leaderboard",
  description: `Get competition leaderboard

Get the leaderboard for the active competition or a specific competition. Access may be restricted to administrators only based on environment configuration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await competitionGetLeaderboard(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
