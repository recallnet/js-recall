/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountGetPortfolio } from "../../funcs/accountGetPortfolio.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$accountGetPortfolio: ToolDefinition = {
  name: "account-get-portfolio",
  description: `Get portfolio information

Get portfolio valuation and token details for the authenticated team`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await accountGetPortfolio(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
