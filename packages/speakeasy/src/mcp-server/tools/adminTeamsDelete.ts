/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminTeamsDelete } from "../../funcs/adminTeamsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApiAdminTeamsTeamIdRequest$inboundSchema,
};

export const tool$adminTeamsDelete: ToolDefinition<typeof args> = {
  name: "admin-teams-delete",
  description: `Delete a team

Permanently delete a team and all associated data`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminTeamsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
