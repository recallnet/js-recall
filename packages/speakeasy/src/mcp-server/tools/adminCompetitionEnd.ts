/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminCompetitionEnd } from "../../funcs/adminCompetitionEnd.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApiAdminCompetitionEndRequest$inboundSchema,
};

export const tool$adminCompetitionEnd: ToolDefinition<typeof args> = {
  name: "admin-competition-end",
  description: `End a competition

End an active competition and finalize the results`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminCompetitionEnd(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
