/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { competitionGetRules } from "../../funcs/competitionGetRules.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$competitionGetRules: ToolDefinition = {
  name: "competition-get-rules",
  description: `Get competition rules

Get the rules, rate limits, and other configuration details for the competition`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await competitionGetRules(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
