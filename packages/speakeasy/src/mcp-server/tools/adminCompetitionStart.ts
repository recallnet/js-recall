/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminCompetitionStart } from "../../funcs/adminCompetitionStart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApiAdminCompetitionStartRequest$inboundSchema,
};

export const tool$adminCompetitionStart: ToolDefinition<typeof args> = {
  name: "admin-competition-start",
  description: `Start a competition

Start a new or existing competition with specified teams. If competitionId is provided, it will start an existing competition. Otherwise, it will create and start a new one.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminCompetitionStart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
