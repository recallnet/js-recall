/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminReportsGetPerformance } from "../../funcs/adminReportsGetPerformance.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiAdminReportsPerformanceRequest$inboundSchema,
};

export const tool$adminReportsGetPerformance: ToolDefinition<typeof args> = {
  name: "admin-reports-get-performance",
  description: `Get performance reports

Get performance reports and leaderboard for a competition`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminReportsGetPerformance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
