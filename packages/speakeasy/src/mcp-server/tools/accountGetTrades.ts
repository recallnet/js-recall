/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountGetTrades } from "../../funcs/accountGetTrades.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$accountGetTrades: ToolDefinition = {
  name: "account-get-trades",
  description: `Get trade history

Get trade history for the authenticated team`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await accountGetTrades(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
