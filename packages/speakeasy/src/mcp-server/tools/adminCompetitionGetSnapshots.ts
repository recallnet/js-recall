/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminCompetitionGetSnapshots } from "../../funcs/adminCompetitionGetSnapshots.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .GetApiAdminCompetitionCompetitionIdSnapshotsRequest$inboundSchema,
};

export const tool$adminCompetitionGetSnapshots: ToolDefinition<typeof args> = {
  name: "admin-competition-get-snapshots",
  description: `Get competition snapshots

Get portfolio snapshots for a competition, optionally filtered by team`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminCompetitionGetSnapshots(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
