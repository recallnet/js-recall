/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { priceGetTokenInfo } from "../../funcs/priceGetTokenInfo.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiPriceTokenInfoRequest$inboundSchema,
};

export const tool$priceGetTokenInfo: ToolDefinition<typeof args> = {
  name: "price-get-token-info",
  description: `Get detailed token information

Get detailed token information including price and specific chain`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await priceGetTokenInfo(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
