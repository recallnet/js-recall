/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountGetProfile } from "../../funcs/accountGetProfile.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$accountGetProfile: ToolDefinition = {
  name: "account-get-profile",
  description: `Get team profile

Get profile information for the authenticated team`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await accountGetProfile(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
