/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publicTeamsRegister } from "../../funcs/publicTeamsRegister.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApiPublicTeamsRegisterRequest$inboundSchema,
};

export const tool$publicTeamsRegister: ToolDefinition<typeof args> = {
  name: "public-teams-register",
  description: `Register a new team

Public endpoint to register a new team. Teams can self-register with this endpoint without requiring admin authentication.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await publicTeamsRegister(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
