import { strictEqual } from "assert";

describe("consensus", function () {
  it("should be able to call cometbft", function () {
    const cometbft = "test";
    strictEqual(cometbft, "test");
  });
});
