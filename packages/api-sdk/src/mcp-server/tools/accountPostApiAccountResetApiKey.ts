/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { accountPostApiAccountResetApiKey } from "../../funcs/accountPostApiAccountResetApiKey.js";
import { ToolDefinition, formatResult } from "../tools.js";

export const tool$accountPostApiAccountResetApiKey: ToolDefinition = {
  name: "account-post-api-account-reset-api-key",
  description: `Reset team API key

Reset the API key for the authenticated team. This will invalidate the current API key and generate a new one.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await accountPostApiAccountResetApiKey(client, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
