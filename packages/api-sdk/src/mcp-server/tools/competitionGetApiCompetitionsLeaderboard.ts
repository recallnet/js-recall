/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { competitionGetApiCompetitionsLeaderboard } from "../../funcs/competitionGetApiCompetitionsLeaderboard.js";
import * as operations from "../../models/operations/index.js";
import { ToolDefinition, formatResult } from "../tools.js";

const args = {
  request: operations.GetApiCompetitionsLeaderboardRequest$inboundSchema,
};

export const tool$competitionGetApiCompetitionsLeaderboard: ToolDefinition<
  typeof args
> = {
  name: "competition-get-api-competitions-leaderboard",
  description: `Get competition leaderboard with sorting

Get the leaderboard for the active competition or a specific competition with optional sorting and pagination. Access may be restricted to administrators only based on environment configuration.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await competitionGetApiCompetitionsLeaderboard(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
