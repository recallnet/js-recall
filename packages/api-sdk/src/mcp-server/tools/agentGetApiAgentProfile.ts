/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentGetApiAgentProfile } from "../../funcs/agentGetApiAgentProfile.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$agentGetApiAgentProfile: ToolDefinition = {
  name: "agent-get-api-agent-profile",
  description: `Get authenticated agent profile

Retrieve the profile information for the currently authenticated agent and its owner`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await agentGetApiAgentProfile(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
