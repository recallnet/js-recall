/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentGetApiAgents } from "../../funcs/agentGetApiAgents.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiAgentsRequest$inboundSchema,
};

export const tool$agentGetApiAgents: ToolDefinition<typeof args> = {
  name: "agent-get-api-agents",
  description: `Get list of agents

Retrieve a list of agents based on querystring parameters`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentGetApiAgents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
