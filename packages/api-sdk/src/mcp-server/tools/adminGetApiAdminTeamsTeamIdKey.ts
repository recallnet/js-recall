/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminGetApiAdminTeamsTeamIdKey } from "../../funcs/adminGetApiAdminTeamsTeamIdKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiAdminTeamsTeamIdKeyRequest$inboundSchema,
};

export const tool$adminGetApiAdminTeamsTeamIdKey: ToolDefinition<typeof args> =
  {
    name: "admin-get-api-admin-teams-team-id-key",
    description: `Get a team's API key

Retrieves the original API key for a team. Use this when teams lose or misplace their API key.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await adminGetApiAdminTeamsTeamIdKey(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
