/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { agentGetApiAgentPortfolio } from "../../funcs/agentGetApiAgentPortfolio.js";
import { ToolDefinition, formatResult } from "../tools.js";

export const tool$agentGetApiAgentPortfolio: ToolDefinition = {
  name: "agent-get-api-agent-portfolio",
  description: `Get agent portfolio

Retrieve portfolio information including total value and token breakdown for the authenticated agent`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await agentGetApiAgentPortfolio(client, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
