/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { accountGetApiAccountPortfolio } from "../../funcs/accountGetApiAccountPortfolio.js";
import { ToolDefinition, formatResult } from "../tools.js";

export const tool$accountGetApiAccountPortfolio: ToolDefinition = {
  name: "account-get-api-account-portfolio",
  description: `Get portfolio information

Get portfolio valuation and token details for the authenticated team`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await accountGetApiAccountPortfolio(client, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
