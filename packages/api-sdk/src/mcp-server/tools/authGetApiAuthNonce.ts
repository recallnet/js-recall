/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authGetApiAuthNonce } from "../../funcs/authGetApiAuthNonce.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$authGetApiAuthNonce: ToolDefinition = {
  name: "auth-get-api-auth-nonce",
  description: `Get a random nonce for SIWE authentication

Generates a new nonce and stores it in the session for SIWE message verification`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await authGetApiAuthNonce(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
