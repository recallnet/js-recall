/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { competitionGetApiCompetitionsUpcoming } from "../../funcs/competitionGetApiCompetitionsUpcoming.js";
import { ToolDefinition, formatResult } from "../tools.js";

export const tool$competitionGetApiCompetitionsUpcoming: ToolDefinition = {
  name: "competition-get-api-competitions-upcoming",
  description: `Get upcoming competitions

Get all competitions that have not started yet (status=PENDING)`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await competitionGetApiCompetitionsUpcoming(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
