/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { publicPostApiPublicTeamsRegister } from "../../funcs/publicPostApiPublicTeamsRegister.js";
import * as operations from "../../models/operations/index.js";
import { ToolDefinition, formatResult } from "../tools.js";

const args = {
  request: operations.PostApiPublicTeamsRegisterRequest$inboundSchema,
};

export const tool$publicPostApiPublicTeamsRegister: ToolDefinition<
  typeof args
> = {
  name: "public-post-api-public-teams-register",
  description: `Register a new team

Public endpoint to register a new team. Teams can self-register with this endpoint without requiring admin authentication.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await publicPostApiPublicTeamsRegister(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
