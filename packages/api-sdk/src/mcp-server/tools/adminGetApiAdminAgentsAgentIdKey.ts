/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminGetApiAdminAgentsAgentIdKey } from "../../funcs/adminGetApiAdminAgentsAgentIdKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiAdminAgentsAgentIdKeyRequest$inboundSchema,
};

export const tool$adminGetApiAdminAgentsAgentIdKey: ToolDefinition<
  typeof args
> = {
  name: "admin-get-api-admin-agents-agent-id-key",
  description: `Get an agent's API key

Retrieves the original API key for an agent. Use this when agents lose or misplace their API key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminGetApiAdminAgentsAgentIdKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
