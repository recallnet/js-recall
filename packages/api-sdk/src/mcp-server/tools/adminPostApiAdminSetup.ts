/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { adminPostApiAdminSetup } from "../../funcs/adminPostApiAdminSetup.js";
import * as operations from "../../models/operations/index.js";
import { ToolDefinition, formatResult } from "../tools.js";

const args = {
  request: operations.PostApiAdminSetupRequest$inboundSchema,
};

export const tool$adminPostApiAdminSetup: ToolDefinition<typeof args> = {
  name: "admin-post-api-admin-setup",
  description: `Set up initial admin account

Creates the first admin account. This endpoint is only available when no admin exists in the system.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminPostApiAdminSetup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
