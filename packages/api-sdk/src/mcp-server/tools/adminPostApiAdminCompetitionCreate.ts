/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminPostApiAdminCompetitionCreate } from "../../funcs/adminPostApiAdminCompetitionCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApiAdminCompetitionCreateRequest$inboundSchema,
};

export const tool$adminPostApiAdminCompetitionCreate: ToolDefinition<
  typeof args
> = {
  name: "admin-post-api-admin-competition-create",
  description: `Create a competition

Create a new competition without starting it. It will be in PENDING status and can be started later.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminPostApiAdminCompetitionCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
