/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { competitionGetApiCompetitionRules } from "../../funcs/competitionGetApiCompetitionRules.js";
import { ToolDefinition, formatResult } from "../tools.js";

export const tool$competitionGetApiCompetitionRules: ToolDefinition = {
  name: "competition-get-api-competition-rules",
  description: `Get competition rules

Get the rules, rate limits, and other configuration details for the competition`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await competitionGetApiCompetitionRules(client, {
      fetchOptions: { signal: ctx.signal },
    }).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
