/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authPostApiAuthLogout } from "../../funcs/authPostApiAuthLogout.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$authPostApiAuthLogout: ToolDefinition = {
  name: "auth-post-api-auth-logout",
  description: `Logout the current user by destroying the session

Clears the session data and destroys the session cookie`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await authPostApiAuthLogout(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
