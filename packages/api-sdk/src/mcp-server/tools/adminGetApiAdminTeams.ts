/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminGetApiAdminTeams } from "../../funcs/adminGetApiAdminTeams.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$adminGetApiAdminTeams: ToolDefinition = {
  name: "admin-get-api-admin-teams",
  description: `List all teams

Get a list of all non-admin teams`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await adminGetApiAdminTeams(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
