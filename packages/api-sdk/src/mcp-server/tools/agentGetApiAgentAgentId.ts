/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { agentGetApiAgentAgentId } from "../../funcs/agentGetApiAgentAgentId.js";
import * as operations from "../../models/operations/index.js";
import { ToolDefinition, formatResult } from "../tools.js";

const args = {
  request: operations.GetApiAgentAgentIdRequest$inboundSchema,
};

export const tool$agentGetApiAgentAgentId: ToolDefinition<typeof args> = {
  name: "agent-get-api-agent-agent-id",
  description: `Get agent by ID

Retrieve the information for the given agent ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentGetApiAgentAgentId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
