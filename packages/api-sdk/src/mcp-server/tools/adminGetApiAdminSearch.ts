/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { adminGetApiAdminSearch } from "../../funcs/adminGetApiAdminSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiAdminSearchRequest$inboundSchema,
};

export const tool$adminGetApiAdminSearch: ToolDefinition<typeof args> = {
  name: "admin-get-api-admin-search",
  description: `Search users and agents

Search for users and agents based on various criteria`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminGetApiAdminSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
