/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { adminGetApiAdminTeamsSearch } from "../../funcs/adminGetApiAdminTeamsSearch.js";
import * as operations from "../../models/operations/index.js";
import { ToolDefinition, formatResult } from "../tools.js";

const args = {
  request: operations.GetApiAdminTeamsSearchRequest$inboundSchema,
};

export const tool$adminGetApiAdminTeamsSearch: ToolDefinition<typeof args> = {
  name: "admin-get-api-admin-teams-search",
  description: `Search for teams

Search for teams based on various criteria like email, name, wallet address, etc.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await adminGetApiAdminTeamsSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
