/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { agentPutApiAgentProfile } from "../../funcs/agentPutApiAgentProfile.js";
import * as operations from "../../models/operations/index.js";
import { ToolDefinition, formatResult } from "../tools.js";

const args = {
  request: operations.PutApiAgentProfileRequest$inboundSchema,
};

export const tool$agentPutApiAgentProfile: ToolDefinition<typeof args> = {
  name: "agent-put-api-agent-profile",
  description: `Update authenticated agent profile

Update the profile information for the currently authenticated agent (limited fields)`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentPutApiAgentProfile(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
